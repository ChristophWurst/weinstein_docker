version: '3.2'
services:
    web:
        image: weinstein/web:latest
        ports:
            - "8080:80"
        env_file: .env.prod
    database:
        volumes:
            - ./maria-prod:/var/lib/mysql

